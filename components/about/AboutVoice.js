import classes from "./AboutVoice.module.css";

const AboutVoice = () => {
  return (
    <section className={classes.header}>
      <h2>Nuestra voz</h2>

      <p>
        Reserva Ancestral celebra la vida y el balance perfecto entre el pasado
        y lo moderno, siendo el punto intermedio entre lo que somos y lo que
        podemos llegar a ser.
      </p>

      <hr />
    </section>
  );
};

export default AboutVoice;
